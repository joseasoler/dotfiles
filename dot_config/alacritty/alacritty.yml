# Alacritty configuration file. See https://github.com/alacritty/alacritty

# ToDo Support for ~ does not seem to be working at the moment.
import:
  -  /home/jose/.config/alacritty/nord-alacritty/nord.yml

scrolling:

  # Maximum number of lines in the scrollback buffer.
  history: 20000

  # Scrolling distance multiplier.
  multiplier: 5

font:
  normal:
    family: Inconsolata
  size: 16.0
